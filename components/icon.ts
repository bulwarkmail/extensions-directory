// Copied from repos/website/src/lib/icon.ts; keep the two in step.
// Every lucide icon takes these props, so the icons match the square system:
// 1.5px stroke, square caps, mitred joins. Sizes are 16, 20 and 24.
export const ICON = {
  strokeWidth: 1.5,
  strokeLinecap: "square",
  strokeLinejoin: "miter",
  "aria-hidden": true,
} as const;
